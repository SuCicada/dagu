shamefully-hoist=true

# esbuild ships its platform binary via optionalDependencies; its install
# script is not required for vitepress to build. pnpm 11 treats an ignored
# build script as a hard error by default, so downgrade it to a warning.
strict-dep-builds=false
