# pnpm 10+ reads build-script allowances from here, not package.json.
# esbuild ships a platform binary that its install script unpacks; without
# this the docs build fails with ERR_PNPM_IGNORED_BUILDS.
onlyBuiltDependencies:
  - esbuild
